# Security Policy

Do not report credentials, recipient IDs, booking references, private addresses, or live itinerary exports in public issues. Use a private channel with the maintainers for a suspected secret exposure or unsafe delivery behavior.

The notification adapters are designed to fail closed. A visible send must remain explicitly authorized by the deployment operator.
